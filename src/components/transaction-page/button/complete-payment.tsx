import { Button } from "react-bootstrap";

export default function CompletePayment () {

    return (
        <Button className='w-full mt-3 p-1 bg-green-400 rounded-xl hover:font-bold hover:bg-green-500'>
            Complete Payment
        </Button>
    );
}