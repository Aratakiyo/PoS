"use client";
import { Button } from "react-bootstrap";

const Back = () => {
  return (
    <Button className="w-1/2 bg-gray-400 rounded-xl items-center hover:font-bold hover:bg-gray-500 text-white h-32 text-xl text-center hover:border-black border-4">
      Back
    </Button>
  );
};

export default Back;
